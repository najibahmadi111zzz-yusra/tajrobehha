<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:label="تجربه‌ها"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>

چرا این نسخه را می‌گذاریم؟
فعلاً "AppTheme" و "ic_launcher" را حذف کردیم تا اگر مشکل از Theme یا آیکون ناقص باشد، برنامه به آنها وابسته نباشد. همچنین Activityهای دیگر را موقتاً حذف کردیم تا فقط "MainActivity" که کدش را بررسی کردیم اجرا شود.

بعد از جایگزینی، هنوز Build نزن. فقط به من بگو: «جایگزین شد»؛ مرحله بعد را با هم انجام می‌دهیم.
